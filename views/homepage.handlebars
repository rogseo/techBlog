{{#each posts as |post|}}
<section class="card">
    <a class="card-body" href="/post/{{id}}">
        <h5 class="card-title">{{title}}</h5>
        <h6 class="card-subtitle mb-2 text-muted">{{user.username}}</h6>
        <p class="card-text">{{description}}</p>
    </a>
</section>
{{/each}}