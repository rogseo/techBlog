<form class="row justify-content-center">
  <div class="form-group col-8">
    <label for="add-title">Title :</label>
    <textarea type="text" class="form-control" id="add-title" >{{post.title}}</textarea>
  </div>
  
  <div class="form-group col-8">
    <label for="add-description">Content : </label>
    <textarea class="form-control" id="add-description" rows="10">{{post.description}}</textarea>
  </div>
  <div class="form-group col-8">
    <button type="submit" class="btn btn-primary" data-id="{{post.id}}" id="edit-post">Edit</button>
    <button type="submit" class="btn btn-primary" data-id="{{post.id}}" id="delete-post">Delete</button>
  </div>
</form>

<script src="http://localhost:3001/js/script.js"></script>